---
name: Project goals
description: Build targets and deployment decisions for hello-world-go
type: project
---

Building a production-template Go REST API incrementally.

Completed: MS OAuth2/Azure AD roles, Postgres (pgx), OpenAPI/Swagger docs, MCP server (SSE), Docker containerization.

**Why:** Learning Go with a real production pattern; reusable template.

**How to apply:** No AWS/ECS — deployment target is Docker (homelab or elsewhere via compose). GitHub Actions CI is still planned (Step 9). Postgres is on homelab at node1.rodenas.us.
